<h3>Comments:</h3>
<hr>
<div class="row">
  {{#each post.comments as |comment|}}
    <div class="col-sm-8">
      <p>{{comment.name}}</p>
      <p>{{comment.email}}</p>
      <p>{{comment.description}}</p>
      <button {{action 'delete' comment}}>Delete Comment</button>
      <!-- {{#if updateCommentForm}}
        <h4>Update {{comment.name}}'s comment</h4>
        <div>
          <form>
            <div class="form-group">
              <label for="name">Name</label>
              {{input value=name id='name1' placeholder=comment.name}}
            </div>

            <div class="form-group">
              <label for="email">Email</label>
              {{input value=email id='email1' placeholder=comment.email}}
            </div>

            <div class="form-group">
              <label for="description">Comment</label>
              {{input value=description id='description1' placeholder=comment.description}}
            </div>

            <button class="btn btn-success" {{action 'update' comment}}>Update</button>
          </form>
        </div>

      {{else}}
        <button class="btn btn-danger" {{action 'showUpdateForm'}}>Update Comment</button>
      {{/if}} -->
      <hr>
    </div>
  {{/each}}
</div>
<div class="row">
  {{#if addNewComment}}
    <div class="col-sm-6 well">
      <form>
        <div class="form-group">
          <label for="name">Name:</label>
          {{input value=name id="name" class="form-control"}}
        </div>
        <div class="form-group">
          <label for="email">Email:</label>
          {{input value=email id="email" class="form-control"}}
        </div>
        <div class="form-group">
          <label for="description">Comment:</label>
          {{textarea cols="80" rows="6" value=description id="description" class="form-control"}}
        </div>
        <button class="btn btn-success" {{action 'addComment'}}>Add Comment</button>
      </form>
    </div>
  {{else}}
    <button class="btn btn-warning" {{action 'showCommentForm'}}>New Comment</button>
  {{/if}}
</div>
